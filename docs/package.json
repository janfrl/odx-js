{
  "name": "docs",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "pnpm metadata:extract && nuxi dev",
    "build": "pnpm metadata:extract && nuxi build",
    "generate": "nuxi generate",
    "preview": "nuxi preview",
    "postinstall": "nuxi prepare",
    "clean": "nuxt clean",
    "metadata:extract": "tsx scripts/extract-metadata.ts"
  },
  "dependencies": {
    "docus": "latest"
  },
  "devDependencies": {
    "@bc8-odx/nuxt": "workspace:*",
    "@nuxt/schema": "^4.4.2",
    "nuxt": "^4.4.2",
    "untyped": "^2.0.0"
  }
}
