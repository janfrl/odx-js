---
title: Referenz
description: Technische Referenz für das Paket @me-tools/odx-core.
navigation:
  icon: i-lucide-file-code
---

`@me-tools/odx-core` ist ein Framework-agnostisches Paket, das die Kern-OData-Logik, gemeinsam genutzte TypeScript-Typen und den Basis-Client `$odata` enthält.

## Wichtige Interfaces

### `ODataQuery`

Wird verwendet, um strukturierte Abfragen für OData-Services zu definieren.

| Eigenschaft | Typ | Beschreibung |
| --- | --- | --- |
| `$select` | `string \| string[]` | Felder, die in die Antwort aufgenommen werden sollen. |
| `$filter` | `string` | OData-Filterausdruck. |
| `$orderby` | `string` | Sortierreihenfolge. |
| `$top` | `number` | Begrenzt die Anzahl der Ergebnisse. |
| `$skip` | `number` | Offset für die Paginierung. |
| `$expand` | `string` | Navigations-Properties, die aufgenommen werden sollen. |

### `ODataResponse<T>`

Der normalisierte Response-Envelope, der vom `$odata` Client zurückgegeben wird.

| Eigenschaft | Typ | Beschreibung |
| --- | --- | --- |
| `value` | `T[]` | Die Entitäts-Sammlung. |
| `@odata.count` | `number \| undefined` | Gesamtzahl, wenn `$count=true` angefordert wurde. |
| `@odata.nextLink` | `string \| undefined` | URL für serverseitige Paginierung. |

## Der `$odata` Client

Ein leichtgewichtiger, Promise-basierter Client, der in jeder TypeScript-Umgebung verwendet werden kann.

```ts
import { $odata } from '@me-tools/odx-core'

// Liste mit Abfrageoptionen
const products = await $odata('https://.../V2Service')
  .entitySet('Products')
  .list({ $select: ['ProductID'], $top: 5 })

// Abruf über Schlüssel
const product = await $odata('https://.../V2Service')
  .entitySet('Products')
  .get(1)

// Erstellen
const created = await $odata('https://.../V2Service')
  .entitySet('Products')
  .create({ ProductName: 'Neues Produkt' /* ... */ })
```

## Interne Utilities

Das Paket exportiert auch Utilities, die intern von `@me-tools/odx-proxy` und `@me-tools/odx-nuxt` verwendet werden:

| Export | Beschreibung |
| --- | --- |
| buildODataUrl | Serialisiert eine ODataQuery in einen URL-Query-String. |
| parseCsrfToken | Extrahiert den CSRF-Token aus einem Response-Header. |
| normalizeSapHeaders | Wendet SAP-spezifische Header-Standardwerte an (sap-client, X-Requested-With). |
