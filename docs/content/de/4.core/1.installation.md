---
title: Installation
description: Installieren Sie @me-tools/odx-core für Framework-agnostische OData-Typen und den Basis-Client.
navigation:
  icon: i-lucide-download
---

`@me-tools/odx-core` ist ein Framework-agnostisches Paket. Verwenden Sie es, wenn Sie Standard-OData-TypeScript-Interfaces oder den leichtgewichtigen `$odata` Fetch-Client außerhalb einer Nuxt-Anwendung benötigen.

:::steps{level="2"}

## Paket installieren

:::code-group
```bash [pnpm]
pnpm add @me-tools/odx-core
```
```bash [npm]
npm install @me-tools/odx-core
```
```bash [yarn]
yarn add @me-tools/odx-core
```
:::

## Typen importieren

Verwenden Sie die gemeinsam genutzten OData-Typen in jedem TypeScript-Projekt.

```ts
import type { ODataQuery, ODataResponse } from '@me-tools/odx-core'

const query: ODataQuery = {
  $select: ['ProductID', 'ProductName'],
  $filter: 'UnitPrice gt 20',
  $top: 10
}
```

## Den `$odata` Client verwenden

Der `$odata` Client ist ein leichtgewichtiger, Promise-basierter HTTP-Client für OData-Services.

```ts
import { $odata } from '@me-tools/odx-core'

const products = await $odata('https://services.odata.org/V2/Northwind/Northwind.svc/')
  .entitySet('Products')
  .list({ $top: 5 })
```

:::

## Wann Core direkt verwendet werden sollte

| Situation | Empfohlenes Paket |
| --- | --- |
| Erstellung einer Nuxt 4 Anwendung | [`@me-tools/odx-nuxt`](/de/nuxt/getting-started) |
| Erstellung einer BFF-Proxy-Schicht | [`@me-tools/odx-proxy`](/de/proxy/installation) |
| Schreiben von geteilten OData-Typen oder Utilities | `@me-tools/odx-core` |
| Integration von OData in eine React/Plain Node App | `@me-tools/odx-core` |
