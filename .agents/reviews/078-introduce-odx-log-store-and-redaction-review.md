# Review: Introduce ODX log store and redaction

Status: complete
Date: 2026-05-08
Reviewer: Codex
Task: `.agents/tasks/done/078-introduce-odx-log-store-and-redaction.md`
Reviewed commit: `c9ef7aa865ad2c6147af8581bb19dbd054bcbeeb`
Integration fix commit: `3fec5e66a84746419e4395951d33647b7ea73851`
Decision: approved

## Findings

None for the focused re-review. The two prior needs-changes findings are
resolved by integration commit `3fec5e66a84746419e4395951d33647b7ea73851`.

## Prior Findings Status

1. [P1] Proxy trace details can still persist sensitive header values:
   resolved. `packages/core/src/dev-logs.ts:177` now sanitizes
   `proxyTrace.details` before payload bounding and storage. The sanitizer
   redacts `injectHeader` details with sensitive `name`/`value`,
   `denyIfHeader` details with sensitive `header` plus `actual`,
   `deniedValue`, or `value`, and nested objects keyed by sensitive header
   names. `packages/proxy/src/utils/rules.ts:152` now includes the checked
   header name in the `HeaderGuard` trace so storage redaction can classify the
   values. Focused tests cover storage-level nested details and rule-generated
   trace entries.

2. [P3] Tracked API reference artifact was not refreshed for the new public log
   exports: resolved. `docs/public/api-reference.json:102` now includes the
   new core log helpers and `docs/public/api-reference.json:491` includes the
   `OdxLogStore` entry generated by the docs API extractor.

## Acceptance Criteria

- [x] Existing local Explorer traffic logs still work with the memory store:
  pass.
- [x] Sensitive headers such as authorization, cookie, set-cookie, API keys,
  SAP session tokens, and CSRF tokens are redacted before storage: pass for the
  focused re-review scope, including `proxyTrace.details` for `injectHeader`,
  `denyIfHeader`, and nested sensitive header-name keys.
- [x] Large request/response bodies are bounded or omitted according to a
  documented policy: pass.
- [x] Store operations are covered by focused tests: pass for append, update,
  list, get, clear, payload policy, and trace-detail redaction coverage.
- [x] Public/core exports remain intentional and documented: pass; root
  `API.md` and the tracked generated API reference artifact are in sync.

## Verification

Run or inspect:

- `git show --stat --oneline --no-renames 3fec5e66a84746419e4395951d33647b7ea73851` - pass; focused fix touched core sanitizer, proxy rule trace detail, focused tests, docs API artifact, and workflow notes.
- `git show --no-ext-diff --unified=80 --no-renames 3fec5e66a84746419e4395951d33647b7ea73851 -- packages/core/src/dev-logs.ts packages/proxy/src/utils/rules.ts packages/proxy/src/utils/trace.ts packages/proxy/test/dev-logs.test.ts packages/proxy/test/rules.test.ts docs/public/api-reference.json` - reviewed.
- `Get-Content`/`Select-String` inspection of `packages/core/src/dev-logs.ts`, `packages/proxy/src/utils/rules.ts`, `packages/proxy/src/utils/trace.ts`, `packages/proxy/src/api/logs.ts`, focused tests, and `docs/public/api-reference.json` - reviewed.
- `pnpm.cmd exec vitest run packages/proxy/test/dev-logs.test.ts packages/proxy/test/rules.test.ts` - sandbox run could not resolve `vitest`; approved rerun outside the sandbox passed, 2 files and 30 tests.
- `pnpm.cmd --filter docs run verify` - pass; regenerated `docs/public/api-reference.json` with no content diff.
- `git diff --check` - pass.

## Residual Risk

- No live SAP BTP/AppRouter smoke test was performed. This is acceptable for
  task 078 because production `/__odx__/logs` remains disabled and AppRouter
  routing was not changed.
- The re-review intentionally covered only the two prior needs-changes findings
  and a scope check. Broader lint/typecheck/proxy package verification were run
  by the implementer/integrator and were not repeated in this focused
  re-review.

## Open Questions

- None.

## Test Gaps

- No actionable test gap remains for the two prior findings.

## Summary

Focused re-review approves task 078. The integration fix redacts sensitive
proxy trace header details at the core storage boundary before persistence,
refreshes the tracked generated API reference artifact, preserves production
`/__odx__/logs` disabled behavior, and does not introduce db0, evlog, metadata
refresh, SDK generation behavior, database migration, Explorer UI redesign, or
an unrelated refactor.

## Next Action

- `.agents/NEXT.md` was updated to start
  `.agents/tasks/ready/079-add-db0-backed-explorer-log-store.md` with a fresh
  Implementer.
- Follow-up task or fix required: no fix for task 078; continue to task 079.
