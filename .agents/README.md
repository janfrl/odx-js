# Agent Configuration

Project-local configuration for AI coding agents.

## MCP Servers

- `nuxt-ui`: Nuxt UI component, composable, template, and documentation tools.
- `docus`: Docus documentation discovery and page retrieval tools.

Both servers use HTTP transport and are configured in `mcp.json`.
